PublicArrayContainer is an abstract class that is a dependency of the PublicArray class
( package:  @writetome51/public-array ).

PublicArrayContainer has no methods, and just 1 public property -- data -- which must be assigned an array.

To include in your project:

import { PublicArrayContainer } from '@writetome51/public-array-container';
