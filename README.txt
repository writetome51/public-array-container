OpenArrayContainer is an abstract class that is a dependency of the OpenArray class
( package:  @writetome51/open-array ).

OpenArrayContainer has no methods, and just 1 public property -- data -- which must be assigned an array.
